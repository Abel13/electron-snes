# Global asset inventory

This directory contains assets shared by the application, such as brand, audio, library defaults,
and input prompts. Console-specific assets live in their console plugin under `plugins/consoles/`.

Console plugins own their visual assets and document attribution in each plugin's `assets/NOTICE.md`.
