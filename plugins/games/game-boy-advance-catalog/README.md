# Game Boy Advance example catalog

This executable metadata plugin contains original, CC0-licensed metadata and cover art for
`Orbit Advance Demo`. It is a catalog fixture only; it does not distribute a ROM.

The `PCGB` game code and `PIXELCORE ADVANCE` header title are the identifiers expected from the
legal homebrew fixture used by integration tests.
